# 새 세션 인수인계 프롬프트

> **아래 「붙여넣을 프롬프트」 블록을 통째로 복사해서 새 채팅 첫 메시지로 보내세요.**
> 그 아래 §2 는 사람이 읽는 배경 설명입니다. 붙여넣지 않으셔도 됩니다.
>
> 작성: 2026-08-23 / 기준 커밋: `d759e0b`

---

## 1. 붙여넣을 프롬프트

```
KDC 메타버스 노코드 교안 프로젝트를 이어서 진행합니다.

【먼저 읽어라 — 이 순서로】
1. HANDOFF.md            전체 현황
2. CLAUDE.md             프로젝트 상시 규칙 (변환 원칙·표현 치환표·마크다운 표기 규칙·금지 사항)
3. plans/00_교안_생성_프롬프트.md   실행 규칙 (실습 설계법·고정 어휘집·자가 검증)
4. plans/05_U6_U7_에디터_작업지시.md  ← 지금 할 일이 여기 있다
5. references/01_unity_engine_nocode.md
   references/01_unity_engine_vod_script.md   골든 레퍼런스 (구조·톤의 기준)

규칙 충돌 시 우선순위: references/ > plans/00 > CLAUDE.md

【문서 형식 — 2026-08-23 에 바뀌었다】
교안은 GitHub 웹에서 그대로 읽는 마크다운이다. MkDocs 사이트 배포는 폐기됐다.
- 강조 블록은  > 🎯 **제목**   (인용 + 이모지 + 굵은 제목)
- 접기는       <details> / <summary>
- mermaid 는 그대로 쓴다 (GitHub 이 기본 지원)
- !!! / ??? / :material-아이콘: 은 쓰지 마라. GitHub 에서 글자 그대로 노출된다
- 링크 경로의 공백은 %20 으로 쓴다. 안 그러면 GitHub 이 링크로 인식하지 않는다
전체 표기 규칙은 CLAUDE.md 의 「마크다운 표기 규칙」에 있다.

【지금 상태 — 2026-08-23】
- 11~21차시  완료 (교안 11 + VOD 11 = 22개 문서)
- 22~27차시  교안 6개 + VOD 2개(22·23). 🟨 19곳이 U-7 검증 대기
- 28~31차시  교안+VOD 8개 완성, 🟨 0곳. 에디터 검증까지 끝남
- 32~33차시  미착수 (U-7 대기)
- 교육용 컴포넌트 21개 제작 완료

【유일한 병목】
U-6(클레이 사격 완성본 씬 제작) → U-7(46항목 조작 검증).
사용자가 Unity 에디터에서 직접 해야 하는 일이다.
절차와 체크리스트는 plans/05 에 전부 있다.

U-7 결과가 나오면 그때 내가 할 일:
  1) ❌ 항목에 맞춰 컴포넌트 수정
  2) 22~27차시 🟨 19곳 확정
  3) VOD 스크립트 4개 작성 (24~27)
  4) Docs/1. NoCode/README.md 의 사격장 표에서 🟨 표시 제거 + VOD 링크 채우기
  5) 32·33차시 착수

【지켜야 할 것】
- 산출물은 차시당 2개: NN_슬러그_nocode.md + NN_슬러그_vod_script.md
- 수강생은 코드를 한 줄도 치지 않는다. 끌어다 놓기·값 바꾸기·목록에서 고르기뿐
- 새 교육용 컴포넌트가 필요하면 임의로 만들지 말고 명세를 제안하고 확인받아라
- 새 용어는 plans/00 §D-1 어휘집에 먼저 등재한 뒤 본문을 쓴다
- 문서를 고쳤으면 python tools/check_docs.py 로 검증한다 (오류 0건이어야 통과)
- 미확정 지점은 🟨 로 표시하고, 확인이 필요한 것은 plans/07 에 적는다

【환경】
따로 설치할 게 없다. Python 표준 라이브러리만 쓴다.
python tools/check_docs.py

【첫 응답으로 해줬으면 하는 것】
위 문서들을 읽고, 현재 상태를 네가 이해한 대로 요약한 뒤
지금 할 수 있는 작업을 제안해라. 바로 파일을 고치지는 마라.
```

---

## 2. 사람이 읽는 배경 — 새 세션에 꼭 알려야 할 것

프롬프트에 다 못 담은, **모르면 같은 실수를 반복할** 내용입니다.
새 세션이 헤매면 아래를 알려주세요.

### 2-1. 이미 밝혀진 것 — 다시 조사하지 마세요

| | 결론 |
| :--- | :--- |
| **XRI 버전** | **3.3.2**. 2.x 와 이름·조작이 다름 (`HANDOFF.md` §4-3 비교표) |
| **XR 리그** | 메뉴로 만들지 말 것. **Starter Assets 프리팹**을 끌어다 놓는다 |
| **이동 부품 위치** | `XR Origin (XR Rig)` ▸ **`Locomotion`** ▸ `Move` · `Turn` · `Teleportation` |
| **시뮬레이터 조작** | 둘러보기 = **마우스 우클릭**, 손 = **`[` `]` `H` 토글**, 잡기 = **`G`**, 순간이동 = **`I`** |
| **순간이동 함정** | `Teleportation Area` 의 **`Interaction Layer Mask` → `Teleport`** 안 하면 빨간 선 |
| **`Move` 끄기** | 껴도 `W A S D` 는 움직임 (시뮬레이터가 기기를 직접 옮김) |
| **22~27 교안 ↔ 컴포넌트** | **대조 완료, 불일치 0건** |

### 2-2. 문서 형식이 바뀌었습니다 (2026-08-23)

**MkDocs 사이트 배포를 폐기했습니다.** 교안 md 파일만 있으면 되기 때문입니다.

| 지워진 것 | |
| :--- | :--- |
| `mkdocs.yml` · `mkdocs.offline.yml` | 사이트 설정 |
| `requirements-docs.txt` | 빌드 의존성 (`pygments` 고정 이슈도 함께 소멸) |
| `preview-docs.ps1` · `site-offline/` | 로컬 미리보기 |
| `.github/workflows/deploy-docs.yml` | 자동 배포 |
| `.pages` × 8 | 사이트 목차 제어 |
| `Docs/1. NoCode/stylesheets/extra.css` | 사이트 전용 CSS |
| `plans/06_배포_공개_절차.md` | 배포 절차 문서 |
| `Docs/1. NoCode/index.md` | → **`README.md`** 로 교체 (GitHub 이 폴더에서 자동으로 띄웁니다) |

**`!!!` / `???` 문법 830곳**은 `> 이모지 **제목**` 과 `<details>` 로 바꿨습니다.
원문은 한 글자도 손실되지 않았습니다 (38개 파일 전수 대조, 불일치 0건).

> ⚠️ **링크 경로의 공백을 조심하세요**
>
> 폴더 이름이 `4. 사격장` 처럼 공백을 갖고 있습니다.
> `[교안](4. 사격장/22_clay_world_nocode.md)` 로 쓰면 **GitHub 이 링크로 인식하지 못합니다.**
> `4.%20사격장/...` 으로 쓰세요. `python tools/check_docs.py` 가 잡아줍니다.

### 2-3. 예전 GitHub Pages 사이트

**내리지 않고 그대로 뒀습니다.** 배포 워크플로를 지웠으므로 더 이상 갱신되지 않고,
**변환 전 상태(11~21차시)로 남아 있습니다.** 내리시려면 저장소
`Settings → Pages` 에서 직접 꺼주셔야 합니다.

### 2-4. U-6 시작 전에 XR 끄기

클레이 사격은 **비VR 씬**인데 지금 프로젝트는 XR 이 켜져 있습니다.
`Project Settings` → `XR Plug-in Management` → **`Initialize XR on Startup` 해제**.
`plans/05` §0-2 에 적어뒀습니다.

---

## 3. 새 PC에서 처음 할 일

```bash
git pull
python tools/check_docs.py
```

오류 0건이면 준비 끝입니다. Unity 는 **6000.3.15f1** 이어야 합니다.

> 📝 **`.meta` 는 이미 정리돼 있습니다**
>
> 예전 판본의 *"한 대의 PC에서만 여세요"* 절차는 **이미 끝났습니다.**
> 다만 **새 에셋을 만들면 `.meta` 를 짝으로 커밋**하는 규칙은 계속 유효합니다 (`HANDOFF.md` §3).
