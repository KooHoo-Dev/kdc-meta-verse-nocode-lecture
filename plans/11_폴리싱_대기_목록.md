# 폴리싱 대기 목록 — 실제 리소스가 들어오면 할 일

> **잊어버리지 않으려고 모아두는 문서입니다.**
> 씬은 다 만들어졌지만 **소리 · 효과 파일이 없어서 비워둔 자리**를 여기에 계속 쌓습니다.
>
> 새 항목이 생기면 **여기에 먼저 적고** 씬·교안에 🟨 를 답니다.
> 작성: 2026-08-23

---

## 1. 한눈에

| # | 무엇이 없나 | 차시 | 지금 상태 | 파일이 오면 |
| :---: | :--- | :---: | :--- | :--- |
| **1** | **총구 불꽃** 효과 | 24 | ⬜ **안 만듦** | 씬에 넣고 `On Fired` 에 연결 |
| **2** | **터지는 효과** (명중) | 25 | ⬜ **안 만듦** | 틀 안에 넣고 `On Hit` 에 연결 |
| **3** | **총소리** | 27 | 🟡 **배선 완료** | `Audio Clip` 칸에 끼우기만 |
| **4** | **명중음** | 27 | 🟡 **배선 완료** | 〃 |
| **5** | **배경음악** | 27 | 🟡 **배선 완료** | 〃 |
| **6** | 하늘 사진 (선택) | 27 · 30 | 🟢 **임시로 됨** | 더 좋은 하늘로 바꾸고 싶으면 |
| **7** | **체력바 그림** | 17 · 18 | 🟢 **임시로 됨** | `Source Image` 칸에 끼우기 |
| **8** | 그림 파일 (선택) | 15 · 16 · 17 | 🟢 **임시로 됨** | 〃 |

> 🟡 **와 ⬜ 는 다릅니다**
>
> | 🟡 배선 완료 | **연결까지 다 돼 있고 파일만 비었습니다.** 칸에 끼우면 바로 됩니다 |
> | :--- | :--- |
> | ⬜ 안 만듦 | **씬에 아예 없습니다.** 물건을 만들고 연결까지 해야 합니다 |

---

## 2. 항목별 자세히

### ① 총구 불꽃 — 24차시

| | |
| :--- | :--- |
| **왜 뺐나** | 기본 도형 파티클로 만들면 **보기에 좋지 않았습니다.** 코드로 만든 것은 분홍색으로도 나왔고요 |
| **지금** | 씬에 없습니다. `SimpleGun` 의 `On Fired` 가 비어 있습니다 (비어 있어도 총은 잘 쏩니다) |
| **어디에** | `Player ▸ Main Camera ▸ Gun` 의 자식. 자리는 `MuzzlePoint` 와 같게 (`0, 0.03, 0.5`) |
| **연결** | `SimpleGun` 의 **`On Fired`** ▸ 그 효과의 `ParticleSystem.Play` |
| **코드로 할 때** | `Build24ClayGun.cs` 의 «총구 불꽃» 절에 자리와 방법을 주석으로 남겨뒀습니다 |
| **교안** | `24_clay_gun_nocode.md` 실습 ④ 에 🟨. **실습 ④ 를 다시 써야 합니다** |
| **VOD** | `24_clay_gun_vod_script.md` **[00:45 ~ 00:47]** 구간에 🟨. 같이 다시 씁니다 |

> 💡 **`MuzzleFlashPlayer` 를 쓸지도 같이 정해야 합니다**
>
> 파티클만 터뜨릴 거면 `ParticleSystem.Play` 로 충분합니다.
> **불빛까지 켜려면** 강의자료의 `MuzzleFlashPlayer` 를 쓰는 쪽이 낫습니다.

### ② 터지는 효과 — 25차시

| | |
| :--- | :--- |
| **왜 뺐나** | ①과 같은 이유입니다 |
| **지금** | 표적 틀에 `FeedbackSpawner` 를 **아예 안 붙였습니다.** 그래서 빈 칸도 없습니다 |
| **어디에** | 효과를 **틀(Prefab)로** 만들어두고, 표적 틀 안에 `FeedbackSpawner` 를 붙입니다 |
| **연결** | `Hittable` 의 **`On Hit`** ▸ `FeedbackSpawner.Spawn` (틀 안끼리라 연결됩니다) |
| **조율값** | `Spawn Offset` — 효과가 접시 자리에 뜨도록 (U-7 `4-7`) |
| **주의** | 효과 틀의 **`Stop Action` 을 `Destroy`** 로 해야 화면에 안 쌓입니다 (U-7 `4-8`) |
| **교안** | `25_clay_hit_score_nocode.md` 실습 ② 와 §6.3 에 🟨 |
| **VOD** | `25_clay_hit_score_vod_script.md` **[00:15 ~ 00:20]** 구간에 🟨. 순서·대사는 그대로 쓸 수 있습니다 |

### ③④⑤ 소리 세 가지 — 27차시

**배선은 전부 끝나 있습니다.** `Audio Clip` 칸만 비어 있어요.

| 소리 | 어디에 붙어 있나 | 무엇에 이어져 있나 | 값 |
| :--- | :--- | :--- | :--- |
| **총소리** | `Gun` 의 `Audio Source` | `SimpleGun` 의 **`On Fired`** | `Volume 0.6` · 한 번만 |
| **명중음** | **표적 틀 안**의 `Audio Source` | `Hittable` 의 **`On Hit`** | `Volume 0.8` · 한 번만 |
| **배경음악** | `BGM` 의 `Audio Source` | (자동 재생) | `Volume 0.3` · **반복** · **바로 재생** |

**할 일은 이것뿐입니다.** 각 `Audio Source` 의 `Audio Clip` 칸에 소리 파일을 끌어다 놓기.

`27_clay_polish_vod_script.md` **[00:27 ~ 00:39]** 구간에 🟨 가 달려 있습니다.
**연결 순서와 값은 확정이라** 소리 파일만 오면 그 🟨 블록만 지우면 됩니다.

> ✅ **소리 파일이 없어도 경고가 안 뜹니다** (2026-08-23 확인)
>
> 빈 `Audio Source` 에 재생을 시켜도 유니티가 조용히 넘어갑니다.
> **수강생이 소리 파일을 안 넣어도 콘솔이 깨끗합니다.**

> 💡 **총소리가 기계처럼 들린다면**
>
> `Audio Source` 대신 강의자료의 **`AudioPlayer`** 를 쓰고 `Random Pitch` 를 `0.1` 쯤 주면
> 쏠 때마다 높낮이가 조금씩 달라집니다. 교안 27차시에 이미 안내가 들어 있습니다.

### ⑦ 체력바 그림 — 17 · 18차시

| | |
| :--- | :--- |
| **지금** | 유니티 내장 **정사각형** 그림(`UI/Skin/Background.psd`)을 쓰고 있습니다 |
| **뭐가 문제인가** | 정사각형을 **`300 × 40`** 으로 늘리니 **비율이 깨져 모양이 안 좋습니다** |
| **왜 그런가** | 채우기(`Filled`) 모드는 9-슬라이스 **테두리를 안 자르고 통째로 늘립니다.** 그래서 모서리가 뭉개집니다 |
| **무엇이 필요한가** | **가로로 긴 직사각형** 그림. `256 × 32` 정도면 충분합니다. 테두리 없는 단색이면 더 깔끔합니다 |
| **어디에** | `HealthBar` 의 **`Source Image`** 칸 |
| **누가 씁니까** | 17차시가 만들고, **18차시 실습 ④** 가 슬라이더로 이 값을 움직입니다 |

> 📝 **처음엔 더 나빴습니다** (2026-08-24)
>
> `UI/Skin/UISprite.psd` 를 썼더니 **모서리가 둥근 그림**이라 늘어나면서 **알약 모양**이 됐습니다.
> 각진 `Background.psd` 로 바꿔 한 단계 나아졌지만, **정사각형이라는 한계는 그대로**입니다.

### ⑧ 그림 파일 — 15 · 16 · 17차시

| | |
| :--- | :--- |
| **어디에 필요한가** | 15차시 실습 ②(밖에서 만든 파일 넣기) · 16차시 `ShowSprite` · 17차시 `DemoImage` |
| **지금** | **유니티 내장 그림**으로 됩니다. **빌더가 그림을 지어내지 않습니다** |
| **왜 급하지 않나** | 교안 목적(입체 ↔ 납작 ↔ 효과 견주기 · 앵커 익히기)이 **내장 그림으로 전부 달성됩니다** |
| **바꾸고 싶다면** | `Texture Type` 을 **`Sprite (2D and UI)`** 로 바꾼 뒤 각 칸에 끼우면 됩니다 |

> 💡 **16차시 스프라이트는 크기를 자동으로 맞춥니다**
>
> `BuildKit.Sprite2D` 가 그림의 **실제 크기를 재서**(`sprite.bounds`) 정한 칸 수(16차시는 1.5칸)로 맞춥니다.
> **그림을 바꿔 끼워도 크기가 그대로**라 다시 조정할 필요가 없습니다.

### ⑥ 하늘 — 27 · 30차시

| | |
| :--- | :--- |
| **지금** | 유니티 내장 **절차적 하늘**로 노을을 만들어뒀습니다. 사진 파일이 없어도 됩니다 |
| **바꾸고 싶다면** | 360 사진(`Cubemap` 또는 `Spherical (HDR)`)으로 만든 재료를 `Lighting ▸ Environment ▸ Skybox Material` 에 |
| **30차시** | 교안 실습 ③은 **360 사진으로 `Panoramic` 재료**를 만들게 합니다 |
| **지금 30차시 씬** | 🟢 **내장 절차적 하늘 세 개**(낮 · 노을 · 밤)로 만들어뒀습니다 |
| **그래서 뭐가 되나** | **실습 ④(버튼으로 갈아 끼우기)는 그대로 성립합니다.** 재료가 사진이든 아니든 같습니다 |
| **사진이 오면** | `Build30Vr360.BuildSkies()` **한 함수만** `Panoramic` 재료를 만들게 고치면 됩니다. 뒤쪽(버튼 · 글자 연결)은 손댈 것 없음 |

---

## 3. 리소스가 들어오면 — 순서

| 순서 | 할 일 |
| :---: | :--- |
| 1 | 파일을 `Assets/99. External Assets/` 아래 정리해서 넣기 |
| 2 | **소리 세 가지**는 씬을 열어 `Audio Clip` 칸에 끼우기 — 배선은 이미 돼 있습니다 |
| 3 | **효과 두 가지**는 빌더(`Build24` · `Build25`)에 만드는 코드를 되살리기 |
| 4 | `Tools ▸ 교안 씬 빌더 ▸ 사격장 전체 다시 만들기` 로 한 번에 반영 |
| 5 | **체력바 그림**은 `Build17UguiCanvas` 의 `MakeFillable` 에서 끼우도록 코드에 넣기 |
| 6 | **교안**의 🟨 (24차시 실습 ④ · 25차시 실습 ② · §6.3) 를 실제 순서로 다시 쓰기 |
| 7 | **VOD 스크립트**의 🟨 (24차시 `[00:45~00:47]` · 25차시 `[00:15~00:20]` · 27차시 `[00:27~00:39]`) 도 같이 |
| 8 | `python tools/check_docs.py` |

> ⚠️ **2번은 씬을 다시 만들면 날아갑니다**
>
> 씬은 **빌더가 만드는 산출물**이라, 손으로 끼운 `Audio Clip` 은 재실행 때 사라집니다.
> **소리 파일이 확정되면 `Build27ClayPolish` 가 직접 끼우도록 코드에 넣어주세요.**
> 그전까지는 손으로 끼운 뒤 씬을 다시 만들지 않는 편이 낫습니다.

---

## 4. 관련 문서

| | |
| :--- | :--- |
| `plans/09_에디터_도구_작업계획.md` | 빌더 · 점검기 설계 |
| `plans/10_차시별_씬_빌더_점검기.md` | 차시별 대응표 |
| `plans/12_갈래A_작업계획.md` | 13 ~ 21차시 빌더 (⑦ · ⑧ 이 여기서 나왔습니다) |
| `plans/05_U6_U7_에디터_작업지시.md` | U-7 검증 항목 (`4-7` · `4-8` 이 ②에 걸려 있습니다) |
