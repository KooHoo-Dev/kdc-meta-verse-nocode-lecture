# 유니티 프로젝트 작업 계획

교안이 전제하는 **교육용 컴포넌트 · 프리팹 · 씬 · 배포물**을 만드는 계획입니다.
`plans/01_교안_작업계획.md` 의 Batch 구분을 그대로 따릅니다.

---

## 1. 현재 상태

**갱신: 2026-08-05.** 아래는 실제 리포지토리를 확인한 결과입니다.

| 항목 | 상태 |
| :--- | :--- |
| Unity 버전 | **6000.3.15f1** (`ProjectSettings/ProjectVersion.txt`) |
| 렌더 파이프라인 | URP 17.3.0 — PC / Mobile 렌더러 분리 세팅 완료 |
| 입력 | Input System 1.19.0 + `InputSystem_Actions.inputactions` 존재 |
| UI | UGUI 2.0.0 (Unity 6는 TMP가 UGUI에 통합) |
| **XR** | ✅ **설치 완료** — XRI **3.3.2** · OpenXR 1.16.1 · XR Management 4.7.0 |
| **XRI 샘플** | ✅ `Starter Assets` · `XR Interaction Simulator` 임포트됨 |
| **한글 폰트** | ✅ Noto Sans KR SDF (`Assets/99. External Assets/`) — **별도 배포 예정** |
| **교육용 컴포넌트** | ✅ **21개** (`Assets/_NoCodeKit/Scripts/`) |
| 씬 | ⬜ `SampleScene` 뿐 — **U-6 · U-3b 미착수** |
| 프리팹 | ⬜ 없음 — **U-5 미착수** |

### 아직 안 만든 것

| # | 항목 | 막고 있는 것 |
| :---: | :--- | :--- |
| **U-6** | 클레이 사격 완성본 씬 | **U-7 검증 → 22~27차시 확정** |
| **U-3a** | `_NoCodeKit` → `.unitypackage` | 13차시 녹화 |
| **U-3b** | 13 ~ 21차시용 씬 | 녹화 |
| **U-5** | 프리팹 샘플 (15차시용) | 15차시 녹화 |

> **교육용 컴포넌트는 더 만들 게 없습니다.** 22개 전부 제작 완료입니다.

### ⚠️ 원본 문서와의 버전 불일치

`Docs/0. Origin/3. XR/01_xr_setup.md` 는 **Unity 2022.3 LTS** 기준입니다.
실제 프로젝트는 Unity 6.3 + XRI 3.x 라 **이름과 구조가 다릅니다.**

**28~31차시 교안은 원본이 아니라 실제 프로젝트에서 확인한 내용으로 썼습니다.**
확인된 차이는 `HANDOFF.md` §4-3 의 비교표에 정리돼 있습니다 (`XR Device Simulator` → `XR Interaction Simulator` 등).

---

## 2. 폴더 구조

```
Assets/
├── _NoCodeKit/                  ← 수강생 배포 대상 (언더바로 최상단 고정)
│   ├── Scripts/                 교육용 컴포넌트 (수강생은 열지 않음)
│   ├── Prefabs/
│   ├── Materials/
│   ├── Audio/
│   └── VFX/
├── _Lectures/                   ← 차시별 씬
│   ├── 13_EditorTour.unity
│   ├── 14_Basics.unity
│   └── ...
├── _ClayShooting/               ← 22~27 클레이 사격 전용
│   ├── Prefabs/
│   ├── Scenes/
│   └── Milestones/              차시별 완료 상태 씬 (진도 복구용)
├── _XR/                         ← 28~33 XR 전용
└── Settings/                    (기존 URP 설정 유지)
```

- 수강생이 만드는 것은 `Assets/` 바로 아래에 두게 안내합니다. **강의 자산과 본인 작업물을 섞지 않는 것**이 15차시(데이터 관리) 실습의 일부입니다.
- `_NoCodeKit/Scripts/` 는 수강생이 **열어볼 일이 없어야** 합니다. 각 차시 "코드 딱 한 번 보기"에서만 강사가 엽니다.

---

## 3. 교육용 컴포넌트 명세

### 3-0. 설계 제약 (전 컴포넌트 공통)

노코드 실습이 성립하려면 아래를 지켜야 합니다.

| 규칙 | 이유 |
| :--- | :--- |
| 버튼에 걸 동작은 **`public void` + 매개변수 없음** | 매개변수가 있으면 `On Click` 드롭다운에서 고르기 어려움. 단일 `float`/`int`/`string`/`bool` 까지만 허용 |
| 조절값은 **`[Header]` 로 묶고 `[Tooltip]` 부착** | 교안의 "조절할 수 있는 값" 표와 1:1 대응돼야 함 |
| 이벤트는 **`public UnityEvent`** | Inspector에 `On Click` 과 똑같이 생긴 칸으로 나와야 함 |
| **Inspector 표시명이 곧 교안 용어** | `rotationSpeed` → `Rotation Speed`. 교안 표와 글자까지 일치시킬 것 |
| 실행 중 값 변경이 **즉시 반영** | 실습 ②(값 조작)의 전제 |
| 참조는 **비어 있어도 오류로 죽지 않게** | 수강생이 칸을 안 채우고 Play를 누름. 빨간 오류 대신 노란 경고 1줄 |

### 3-1. Batch A (즉시 필요)

| 컴포넌트 | 차시 | 조절할 수 있는 값 | 버튼에 걸 동작 | 이벤트 |
| :--- | :---: | :--- | :--- | :--- |
| **CubeSpinner** | 14 · 19 | Rotation Speed · Is Spinning · Target Color | StartSpin · StopSpin · ChangeColor | On Stopped |
| **ConsoleTester** | 13 | Message · Log Type (Info/Warning/Error) | SendLog | — |

> 두 컴포넌트의 명세는 **이미 배포된 교안에 확정돼 있습니다.** 항목명·동작명을 바꾸면 교안을 같이 고쳐야 합니다.

### 3-2. Batch B

| 컴포넌트 | 차시 | 조절할 수 있는 값 | 버튼에 걸 동작 | 이벤트 |
| :--- | :---: | :--- | :--- | :--- |
| **RendererSwitcher** | 16 | Mesh Object · Sprite Object · Particle Object | ShowMesh · ShowSprite · PlayParticle · HideAll | — |
| **ValueDisplay** | 18 | Prefix · Suffix · Decimals | SetValue(float) ← 슬라이더에 연결 | — |
| **TransformDriver** | 20 | Move Step · Rotate Step · Scale Step | MoveX± · MoveY± · MoveZ± · RotateY± · ScaleUp · ScaleDown · ResetAll | On Changed |
| **CollisionReporter** | 21 | Target Tag · Report To Console | — | On Hit · On Leave |

### 3-3. Batch C — 클레이 사격 (14개) ✅ **제작 완료**

> ✅ **아래는 계획이 아니라 실제 소스에서 뽑은 것입니다 (2026-08-05 대조)**
>
> `Assets/_NoCodeKit/Scripts/ClayShooting/` 의 `public` 항목과 **글자까지 일치**합니다.
> **Inspector 표시명이 곧 교안 용어**이므로, 이 표를 고치면 교안도 같이 고쳐야 합니다.

| 컴포넌트 | 차시 | 조절할 수 있는 값 | 버튼에 걸 동작 | 이벤트 |
| :--- | :---: | :--- | :--- | :--- |
| **PlayerMover** | 22 | Move Speed · Jump Force · Can Move | EnableMove · DisableMove | — |
| **MouseLook** | 22 | Sensitivity · Invert Y · Min/Max Angle · **Lock Cursor** | LockCursor · UnlockCursor | — |
| **GameStarter** | 23 | Trigger Tag · **Start Once** | StartGame · **EndGame** · ResetGame | On Game Start · **On Game End** |
| **TargetLauncher** | 23 | Target Prefab · **Launch Point** · Interval · Launch Force · Spread Angle | StartLaunching · StopLaunching · **LaunchOne** | On Launched |
| **TargetLife** | 23 | Life Time | — | On Expired |
| **SimpleGun** | 24 | Bullet Prefab · Muzzle Point · Bullet Speed · Fire Rate · **Can Fire** | Fire · **EnableFire** · **DisableFire** | On Fired |
| **MouseShooter** | 24 | **Aim At Crosshair** · Aim Distance · **Fire Button** · Hold To Fire | — | — |
| **Projectile** | 24 | Life Time · Target Tag | — | On Hit · **On Miss** |
| **MuzzleFlashPlayer** | 24 | Particle · Flash Light · Duration | Play | — |
| **HitScorer** | 25 | **Points Per Hit** | AddScore · ResetScore | **On Score Changed (숫자)** |
| **Hittable** | 25 | Hitter Tag · Destroy On Hit · Add Score | — | On Hit |
| **FeedbackSpawner** | 25 | Effect Prefab · **Spawn Offset** | Spawn | — |
| **RoundTimer** | 26 | Duration · **Auto Start** | StartTimer · StopTimer · ResetTimer | **On Tick (숫자)** · On Finished |
| **DifficultyPreset** | 26 | Launcher · Timer · Easy/Normal/Hard · Apply Normal On Start | ApplyEasy · ApplyNormal · ApplyHard | **On Difficulty Changed (글자)** |
| **AudioPlayer** ⚠️ | 27 | Clip · Volume · **Random Pitch** | Play · Stop | — |

> ⚠️ **`MuzzleFlashPlayer` · `AudioPlayer` 는 버려질 수 있습니다.**
>
> `ParticleSystem.Play()` · `AudioSource.Play()` 가 `On Click` 드롭다운에 나오면 불필요합니다.
> **U-7 `3-6` · `6-2`** 로만 답이 나옵니다. (`HANDOFF.md` §7 함정 ③)

> ⚠️ **예전 판본에서 바뀐 것 — 다시 되돌리지 마세요**
>
> | 항목 | 예전 계획 | **확정** | 왜 |
> | :--- | :--- | :--- | :--- |
> | 26차시 난이도 | 별도 컴포넌트 **없이** 값만 조정 | **`DifficultyPreset` 버튼 3개** | `HANDOFF` §6 결정 6 — 값 입력보다 게임다움 |
> | `Projectile` | `Speed` 를 가짐 | **`Speed` 없음** | 속도를 두 곳에 두면 어느 쪽이 이기는지 헷갈림. **`SimpleGun.Bullet Speed` 한 곳**에서만 정함 |
> | `HitScorer` | `Target Tag`·`Points` / `On Scored` | **`Points Per Hit` / `On Score Changed`** | 명중 판정은 `Hittable` 이 맡고, 점수만 셈 (함정 ①) |
> | `FeedbackSpawner` | `Life Time` | **`Spawn Offset`** | 효과가 스스로 사라지는 건 `Stop Action = Destroy` 로 처리 |
> | `AudioPlayer` | `Loop` | **`Random Pitch`** | 매번 같은 총소리는 어색함 |

### 3-4. Batch D · E

| 컴포넌트 | 차시 | 상태 | 조절할 수 있는 값 | 버튼에 걸 동작 | 이벤트 |
| :--- | :---: | :---: | :--- | :--- | :--- |
| **SkyboxSwitcher** | 30 | ✅ **제작 완료** | Skyboxes (Label + Material) · Start Index | Next · Previous · **Apply(int)** | **On Skybox Changed (글자)** |
| ~~**VRGunHandler**~~ | ~~32~~ | ❌ **취소** | `XR Grab Interactable` 의 **`Activated`** 칸에 `SimpleGun.Fire` 를 **그대로 연결**하면 됩니다. 어댑터가 필요 없습니다 (결정 10) | | |

> 28 · 29 · 31 · **32차시는 XRI 내장만으로 실습이 성립**합니다. 새로 만들 것 없음.
> 32차시는 24차시의 `MouseShooter` 를 **떼어내는** 것으로 처리합니다 (`plans/08` §5).

> 📝 **`SkyboxSwitcher` 는 아직 에디터에서 안 돌려봤습니다**
>
> 소스는 완성됐지만 **실행 검증 전**입니다. `plans/07` **Q9** 를 참고하세요.

---

## 4. 프리팹 · 씬 · 에셋

### 4-1. 프리팹

| 프리팹 | 최초 등장 | 만드는 방식 |
| :--- | :---: | :--- |
| `Car` | 14 → 15 | 수강생이 14차시에 만든 자동차를 15차시에 프리팹화. **강사 배포본은 정답지 용도** |
| `Target` | 23 | Cylinder 납작하게 (클레이 피전) + `TargetLife` |
| `TargetLauncher` | 23 | Cube 조합 + `TargetLauncher` |
| `StartObject` | 23 | Cube + Collider(Trigger) + `GameStarter` |
| `Bullet` | 24 | Sphere 작게 + `Projectile` |
| `Gun` | 24 | Cube 3개 조합 (총열·몸통·손잡이) — **14차시 자동차와 같은 방식** |
| `HitEffect` | 25 | Particle System + `FeedbackSpawner` 대상 |
| `ShootingRange` | 33 | 22~27 결과물 전체를 한 프리팹으로. **33차시의 핵심 소재** |
| ~~`XR Origin (NoCode)`~~ | ~~29~~ | ❌ **만들지 않습니다.** Starter Assets 의 **`XR Origin (XR Rig)`** 를 그대로 씁니다 |

> ⚠️ **XR 리그는 직접 만들지 마세요**
>
> `GameObject > XR > XR Origin (VR)` 메뉴로 만든 것은 **`XROrigin` + `InputActionManager` 둘뿐**입니다.
> **`Locomotion Mediator` 가 없어서** 이동 부품을 붙여도 콘솔 에러가 나고 안 걸립니다.
>
> Starter Assets 의 프리팹에는 **걷기·돌기·순간이동·잡기가 처음부터 다** 들어 있습니다.
> → `Assets/Samples/XR Interaction Toolkit/3.3.2/Starter Assets/Prefabs/XR Origin (XR Rig).prefab`

### 4-2. 씬

- **차시별 시작 씬**을 제공합니다. 진도를 놓친 수강생이 그 차시부터 다시 붙을 수 있어야 합니다.
- 클레이 사격(22~27)은 **차시 완료 상태 씬 6개**를 `_ClayShooting/Milestones/` 에 따로 둡니다.

### 4-3. 에셋 조달 — 기본 도형 위주

| 필요한 것 | 조달 방법 |
| :--- | :--- |
| 사격장 · 총 · 표적 · 발사기 | **Unity 기본 도형 조합.** 14차시 자동차와 동일한 방식이라 커리큘럼이 이어짐 |
| 파티클 (명중·머즐 플래시) | Unity 기본 Particle System + 기본 파티클 텍스처 |
| 재질 | URP Lit 로 직접 제작 (13차시에 배운 방식 그대로) |
| 효과음 · BGM | **CC0 무료 3~4개만** (발사 · 명중 · 실패 · BGM) |
| 360 배경 (30차시) | **CC0 HDRI 1~2장** |
| **한글 TMP 폰트** ⚠️ | **필수.** TMP 기본 폰트는 한글이 `□□□` 로 나옵니다. Noto Sans KR 등 OFL 폰트로 **SDF 에셋을 미리 만들어 배포**해야 합니다 |

> 마지막 항목은 18차시(TextMeshPro)에서 반드시 터집니다. **Batch B 착수 전에 준비**하세요.

---

## 5. 패키지 추가 계획

✅ **전부 설치 완료** (2026-08-05)

| 패키지 · 샘플 | 버전 | 용도 |
| :--- | :---: | :--- |
| `com.unity.xr.interaction.toolkit` | **3.3.2** | XRI 본체 |
| `com.unity.xr.management` | 4.7.0 | XR Plug-in Management |
| `com.unity.xr.openxr` | 1.16.1 | OpenXR 런타임 |
| XRI 샘플 — **Starter Assets** | — | **`XR Origin (XR Rig)` 프리팹이 여기 있습니다** |
| XRI 샘플 — **XR Interaction Simulator** | — | 실기기 없이 실습 |

> `com.unity.inputsystem` 은 **원래부터 들어 있습니다** (1.19.0). 따로 설치 안 함.

- **수강생 배포 시점 주의** — Batch A~C 수강생 프로젝트에는 XR 패키지가 **없어야** 합니다. 무거워집니다.
  강의자료 `.unitypackage` 를 **차시별로 나눠** 내보내세요.
- 28차시 교안에 **버전 번호를 본문에 쓰지 않습니다.** "강의자료에 안내된 버전" 으로 통일합니다.

---

## 6. 배포 방식

| 배포물 | 형태 | 갱신 시점 |
| :--- | :--- | :--- |
| **`NoCodeKit.unitypackage`** | 교육용 컴포넌트 + 공용 재질 + TMP 한글 폰트 | Batch 완료마다 누적 갱신 |
| **차시 시작 씬** | 차시별 `.unitypackage` | 해당 차시 확정 시 |
| **클레이 사격 마일스톤** | 22~27 완료 상태 6개 | Batch C 완료 시 |
| **VR 프로젝트 설정 가이드** | 문서 (28차시 교안에 통합) | Batch D |

- 수강생 안내는 **"Project 창에 끌어다 놓기"** 한 가지로 통일합니다. 15차시에서 배우는 방식 그대로입니다.
- `.unitypackage` 는 리포지토리에 커밋하지 않습니다 (용량). **GitHub Releases 또는 별도 배포처**를 사용하고, 교안에서는 "강의자료" 로만 지칭합니다.

---

## 7. 클레이 사격 제작 방식 — 역분할

교안 6차시가 프로젝트에 묶여 있으므로, 순서를 뒤집습니다.

```
1) 게임을 끝까지 한 번 완성한다 (코드 유무 신경 쓰지 않고)
        ↓
2) 완성본을 6개 마일스톤으로 역분할한다
        ↓
3) 각 마일스톤 사이의 차이 = 그 차시의 실습 ③
        ↓
4) 그 차이를 만들어내는 조작이 마우스만으로 되는지 검증한다
        ↓  안 되면
5) 부족한 부분을 교육용 컴포넌트로 흡수한다
        ↓
6) 교안 집필
```

| 마일스톤 | 차시 | 그 시점의 상태 |
| :---: | :---: | :--- |
| M1 | 22 | 걸어 다닐 수 있는 빈 사격장 |
| M2 | 23 | 표적이 날아옴 (아직 못 맞힘) |
| M3 | 24 | 총알이 나감 (아직 안 맞음) |
| M4 | 25 | 맞으면 터지고 점수가 오름 — **게임 성립** |
| M5 | 26 | 난이도 3단계 |
| M6 | 27 | 배경·조명·소리가 붙은 완성본 |

> **4단계 검증이 핵심입니다.** "이 조작이 마우스만으로 되는가"를 통과하지 못하면 그 실습은 성립하지 않습니다. 통과 못 하는 부분이 곧 새 교육용 컴포넌트의 명세가 됩니다.

---

## 8. 작업 순서

| 순서 | 작업 | 산출물 | 선행 |
| :---: | :--- | :--- | :--- |
| 1 | `CubeSpinner` · `ConsoleTester` 제작 | 스크립트 2개 | — |
| 2 | 13·14차시 씬 + `NoCodeKit` v1 | `.unitypackage` | 1 |
| 3 | TMP 한글 폰트 SDF 제작 | 폰트 에셋 | — |
| 4 | Batch B 컴포넌트 4개 + 프리팹 | `NoCodeKit` v2 | 1 |
| 5 | **클레이 사격 완성본 제작** | 씬 1개 | 4 |
| 6 | 마일스톤 6개로 역분할 + 마우스 조작 검증 | 씬 6개 · 컴포넌트 14개 | 5 |
| 7 | `NoCodeKit` v3 | `.unitypackage` | 6 |
| 8 | XR 패키지 설치 + Simulator 샘플 세팅 (녹화 환경 구축) | XR 프로젝트 | — |
| 9 | XR Origin 프리팹 + 30·31차시 씬 | `NoCodeKit` v4 | 8 |
| 10 | VR 클레이 사격 재구성 | 씬 2개 | 7 · 9 |
| 11 | 11차시 완성작 투어 영상 소재 | 플레이 영상 | 7 · 10 |

> **8번은 순서와 무관하게 지금 당장 해볼 수 있고, 빨리 할수록 좋습니다.** 결과에 따라 29·31차시 실습 설계가 통째로 바뀝니다.

---

## 9. 검증 항목

각 컴포넌트를 만들 때 아래를 확인합니다.

- [ ] Inspector 표시명이 **교안 표의 글자와 일치**하는가
- [ ] 버튼 `On Click` 드롭다운에 동작이 **전부 보이는가**
- [ ] 참조 칸을 **비운 채로 Play** 해도 빨간 오류로 죽지 않는가
- [ ] Play 중에 값을 바꾸면 **즉시 반영**되는가
- [ ] 부품을 **체크 해제**하면 동작이 멈추는가 (1차시 "붙이면 생기고 빼면 사라진다"의 근거)
- [ ] 프리팹을 씬에 **여러 개 놓아도** 각자 독립 동작하는가
- [ ] `_NoCodeKit` 만 임포트한 **빈 프로젝트에서도** 동작하는가
