# CLAUDE.md — 교안 노코드 변환 프로젝트

## 프로젝트 목적

프로그래머 대상으로 작성된 Unity 교안(마크다운)을 **비프로그래머·노코드 기반의 공개 교안 + VOD 녹화 스크립트 세트**로 변환한다.

- **변환 대상 수강생**: 프로그래밍 경험 없음, Unity 완전 처음
- **강의 형태**: VOD 녹화 (화면 공유 단독, 강사 얼굴 없음, 실시간 상호작용 없음), 1차시 = 약 50분 단일 영상, 중간 휴식 없음
- **학습 방식**: 코드는 한 줄도 치지 않는다. 완성본 스크립트를 제공하고, **드래그 앤 드롭 + Inspector 값 조작 + 이벤트 연결**만으로 실습한다
- **교안 렌더링**: **GitHub 웹에서 읽는 마크다운 파일** (사이트 빌드 없음). mermaid 사용 가능, 강조 블록은 인용 + 이모지 표기 — 아래 「마크다운 표기 규칙」 참조

## 입력 / 출력

| 구분 | 파일 | 설명 |
| :--- | :--- | :--- |
| 입력 | `NN_챕터명.md` | 프로그래머 대상 원본 교안 |
| 출력 1 | `NN_챕터명_nocode.md` | 비프로그래머용 노코드 교안 |
| 출력 2 | `NN_챕터명_vod_script.md` | 강사용 VOD 녹화 스크립트 |

**골든 레퍼런스** (반드시 먼저 읽고 구조·톤·수위를 그대로 따를 것):
- `references/01_unity_engine_nocode.md` — 교안 변환의 기준
- `references/01_unity_engine_vod_script.md` — 스크립트 변환의 기준

레퍼런스와 이 문서의 규칙이 충돌하면 **레퍼런스가 우선**이다. 레퍼런스는 사용자가 직접 최종 수정한 결과물이다.

---

## 공통 변환 원칙 (두 산출물 모두 적용)

### 1. 순서를 뒤집는다: 동작 먼저, 코드는 마지막에 잠깐

원본 교안은 "코드를 읽고 → 동작을 이해"하는 구조다. 변환본은 반드시 **"동작을 보고 → 코드는 마지막에 구경만"** 구조로 재배열한다.

- 코드 블록은 챕터 마지막의 "코드 딱 한 번 보기" 성격의 섹션에서만, **인스펙터 항목 ↔ 코드 줄 대응 관계**를 보여주는 용도로만 최소 노출한다
- 대응 관계 예시 (이 형식을 따를 것):
  ```
  public float rotationSpeed  ──→  [ Rotation Speed : 90 ]
  ```
- 그 외 모든 코드 블록은 **삭제**한다. 코드로 설명하던 개념은 Inspector 조작·비유·mermaid로 대체한다

### 2. 표현 치환표 (전 문서 강제 적용)

| 쓰지 말 것 | 이렇게 쓸 것 |
| :--- | :--- |
| 변수, 필드 | **입력 칸**, **값** |
| 메서드, 함수 | **동작**, **기능** |
| public / private | (언급하지 않음) |
| 클래스, 인스턴스, 참조 | **끌어다 놓은 것** |
| 컴포넌트 | **부품** (영어 이름은 필요할 때만 병기) |
| 프로퍼티 노출 | **인스펙터에 보이게 하기** |
| 이벤트 구독, 콜백, 델리게이트 | **~할 때 같이 실행되게 연결** |
| 컴파일 | **저장하면 Unity가 알아서 반영** |
| 매개변수 | (다루지 않음) |
| 상속 | (다루지 않음) |
| 프레임 | (다루지 않음) |
| 크로스 플랫폼 | **한 번 만들면 어디든 간다** |
| 렌더링 | **화면에 그리기** |
| 빌드 | **완성해서 내보내기** |

- Unity UI에 실제로 표시되는 영어 이름(Transform, Rigidbody, Inspector, Hierarchy 등)은 **한글 설명을 먼저 말한 뒤 실제 이름을 병기**한다. 예: "위치 부품 (Transform)"
- `float`, `void`, `bool`, `=>` 등 문법 요소는 화면에 보이더라도 **절대 설명하지 않는다**

### 3. 삭제 대상 (원본에 있어도 변환본에서 제거)

- 모든 C# 코드 블록 (위 1번의 대응 관계 노출 제외)
- 상속 개념 및 상속 기반 비유 (예: '총 든 좀비') → **레고 조립 비유**로 대체
- `Start()` / `Update()` / `Time.deltaTime` / `MonoBehaviour` 등 라이프사이클·클래스 설명 → "다음 차시에서 다룹니다"로 미룸
- IL2CPP, DOTS, Burst Compiler 등 성능 최적화 용어 → "지금 걱정할 단계가 아니다, 만드는 게 먼저다"로 결론만
- SOLID, 단일 책임 원칙 등 설계 용어 → "부품 하나는 일 하나만" 수준으로 치환
- 구체적인 라이선스 금액·요금제명 → "일정 규모 이하는 무료" + 공식 페이지 링크로 대체 (VOD는 오래 남고 정책은 바뀐다)
- 트러블슈팅 예고성 불안 유발 문구 (예: "에디터에서 되던 게 빌드하면 안 되는 경우가 많다") → 아직 겪지 않을 문제는 언급하지 않는다

### 4. 심리적 안전 장치 (반드시 포함)

- **첫 문단에 "코드를 한 줄도 치지 않습니다"를 배치**한다. 순서를 바꾸지 않는다
- 복잡한 화면·표가 처음 나올 때마다 **"지금은 몰라도 됩니다"를 명시**한다
- 실습 안내에는 "안 되셔도 괜찮습니다", "마음껏 망가뜨리셔도 됩니다" 류의 문구를 포함한다
- 마무리에는 "충분히 잘하셨어요" 류의 성취감 문구를 포함한다
- "어렵지 않아요"보다 "안 하셔도 됩니다"가 효과적이다 — 불안은 난이도가 아니라 '뭘 해야 할지 모름'에서 온다

---

---

## 마크다운 표기 규칙 (전 문서 공통)

교안과 스크립트는 **GitHub 웹에서 그대로 읽힌다.** 별도 사이트 빌드는 없다.
따라서 GitHub 마크다운이 렌더링하지 못하는 문법은 쓰지 않는다.

### 강조 블록 — 인용(`>`) + 이모지 + 굵은 제목

```markdown
> 🎯 **목표**
>
> 표적이 하늘로 날아오르게 만듭니다.
```

| 성격 | 표기 | 주 용도 |
| :--- | :--- | :--- |
| 실습 목표 | `> 🎯 **목표**` | 실습 ①~④의 첫 블록 |
| 확인·요약 | `> ✅ **제목**` | 세 줄 요약 · 그림 해설 |
| 주의 | `> ⚠️ **제목**` | 잘 안 되신다면 |
| **데이터 손실급 함정** | `> 🚨 **제목**` | **Play 중 값 소실 전용** |
| 도움말 | `> 💡 **제목**` | |
| 안내 | `> ℹ️ **제목**` | 이 자료는 이렇게 보세요 |
| 메모 | `> 📝 **제목**` | |
| 예시 | `> 🧩 **제목**` | |
| 인용·대비 | `> 💬 **제목**` | 직전 차시와 오늘의 차이 |

**규칙**

- 제목 줄 다음에 **`>` 만 있는 빈 줄**을 반드시 한 줄 넣는다 (없으면 제목과 본문이 붙는다)
- 블록 안의 **모든 줄**에 `> ` 를 붙인다 — 표·체크박스·목록도 예외 없다
- 중첩할 때는 `> > ` 를 쓴다
- 강조 블록 안에 **mermaid·코드블록을 넣지 않는다** (인용 안에서는 렌더링되지 않는다)
- `🚨` 는 **데이터 손실급에만** 쓴다. 남발하면 진짜 경고가 묻힌다

### 접기 — `<details>`

```markdown
<details>
<summary>✅ 정답 보기</summary>

정답 내용...

</details>
```

- `<summary>` 다음에 **빈 줄이 반드시 있어야** 안쪽 마크다운이 렌더링된다
- 닫는 `</details>` 앞에도 빈 줄을 넣는다
- 쓰는 곳은 두 군데 고정: **`✅ 확인해보세요`** (실습) · **`✅ 정답 보기`** (정리 문제)

### 쓰지 않는 문법

`!!!` / `???` (admonition) · `=== "탭"` · `{ .class }` · `:material-아이콘:` · `<div class="grid cards">`
— 전부 사이트 전용이라 GitHub에서는 글자 그대로 노출된다.

**mermaid 는 GitHub 이 기본 지원하므로 계속 쓴다.**

## 출력 1 — 노코드 교안 변환 규칙

`references/01_unity_engine_nocode.md` 의 구조를 기준으로 하되, 원본 교안의 섹션 구조에 얽매이지 말고 **자유롭게 재구성**한다.

### 필수 구성 요소 (순서 포함)

1. **제목**: `# **N차시. <친근한 제목>**` — 원본의 기술적 제목 대신 수강생 관점 제목
2. **`> ℹ️ **이 자료는 이렇게 보세요**`**: 영상과 함께 보는 자료임을 안내, 코드 없음 명시, 오늘 할 조작(끌어다 놓기·숫자 바꾸기·목록에서 고르기 등) 예고
3. **`## 오늘의 목표`**: 결과물 중심 목표 + 학습 흐름 mermaid (①→②→③→④)
4. **개념 섹션들**: 원본의 이론 내용을 비유·표·mermaid로 재구성
5. **실습에 쓸 부품 소개 섹션**: 해당 차시의 스크립트가 Inspector에 노출하는 항목을 표로 정리 (조절할 수 있는 값 / 버튼에 연결할 수 있는 동작 / 이벤트 칸)
6. **`## 실습 — 직접 해보세요`**: 실습 ①~④를 번호 순서 리스트로. 각 실습은 `> 🎯 **목표**` → 단계별 순서(결과를 `→` 로 병기) → `<details>` 접기 `✅ 확인해보세요` → `> ⚠️ **잘 안 되신다면**` 구조
7. **`## 오늘 배운 것을 그림으로`**: 전체 구조 mermaid + `> ✅` 해설
8. **`## 정리 문제`**: 원본의 실습 문제를 노코드 기준으로 재작성, `<details>` 접기 `✅ 정답 보기` 유지. "맞히는 게 목적이 아닙니다" 안내 포함
9. **`## 앞으로 조심할 것`**: 원본 주의사항 중 노코드 수강생에게 해당하는 것만
10. **`## 오늘의 정리`**: `> ✅ **세 줄 요약**` + 성취감 문구
11. **`## 다음 차시 준비`**: 다음 차시 예고 + 준비물 체크박스
12. **`## 부록 — 오늘 나온 용어 정리`**: 이번 차시에 등장한 영어 이름 ↔ 우리말 대응표

### 스타일 규칙

- 강조 블록·접기는 위 「마크다운 표기 규칙」의 표기를 그대로 쓴다
- **`> 🚨` 는 데이터 손실급 함정에만** 사용 (예: Play 중 값 소실)
- mermaid는 개념 구조·흐름 표현에 적극 사용. 노드 안에 영어 이름과 한글 설명을 `<br/>`로 병기
- 표를 적극 사용하되, 표 헤더도 수강생 관점으로 ("이렇게 하고 싶다면 | 이 부품을 붙입니다")
- 실습 단계의 클릭 대상은 **굵게 + 백틱** 혼용으로 시각적으로 구분 (레퍼런스 참고)
- 문체: ~합니다 / ~하겠죠? / ~예요 체. 친근하되 과하게 가볍지 않게

---

## 출력 2 — VOD 스크립트 변환 규칙

`references/01_unity_engine_vod_script.md` 의 구조를 기준으로 한다. **이 레퍼런스는 사용자가 군더더기를 직접 덜어낸 버전이므로, 아래 '넣지 않는 것' 목록을 특히 엄수한다.**

### 필수 구성 요소 (순서 포함)

1. **제목**: `# **N차시. <제목> — VOD 녹화 스크립트**`
2. **타임라인 표**: 문서 최상단. `| 시각 | 구간 | 화면 | 비고 |` — 실습 구간과 핵심 구간을 비고에 표시. 총 50분 기준
3. **구간별 본문**: `# **[MM:SS ~ MM:SS] 구간명**` 형식. 각 구간은:
   - `🖥 **화면**:` — 이 시점에 무엇이 떠 있어야 하는지
   - `🗣` + 인용 블록 — 실제 발화 대본 (그대로 읽을 수 있는 수준의 완성 문장)
   - 시연 단계는 번호 리스트 + 각 단계에 🗣 대사 병기
   - `## **⏸ 실습 N (MM:SS ~ MM:SS)**` — 정지 안내 대사 + 실습 순서 코드블록
4. **실습 순서 코드블록**: 각 실습 정지 직후에 배치. 형식:
   ```
   실습 ①
   ① Hierarchy 우클릭 → Create Empty
   ② ...
   ```
5. **마무리 구간**: 세 줄 요약 대사 + 성취감 문구 + 다음 차시 예고 + 설치 안내
6. **"분량이 남는다면"**: 한 줄 처리 — 실습에서 값을 더 다양하게 바꿔보는 장면을 늘리라는 안내
7. **`# 예상 질문 & 미리 심어둘 답변`**: `| 예상 질문 | 처리 지점 |` 표. VOD는 질문을 못 받으므로 영상 안에 답을 심는다는 전제
8. **`# 비프로그래머 대상 표현 치환표`**: 공통 원칙의 치환표를 문서 말미에 재수록

### 대사 작성 규칙

- 톤: ~합니다 / ~하겠죠? / ~한다면 이렇습니다 체. 너무 딱딱하지 않게
- **질문형 도입 금지**: "질문 하나 드릴게요" → "~라고 생각해보세요"로 치환 (답할 사람이 없다)
- 되짚기 안내: "안 되시면 손 드세요" 대신 **"이 부분이 안 잡히면 (몇 분 지점 / 어느 파트)를 다시 보세요"**
- 핵심 메시지는 대사 안에서 명시적으로 반복: "코드를 단 한 줄도 쓰지 않았습니다", "붙이면 생기고, 빼면 사라진다"
- 정지 안내 대사에는 반드시 "다 하셨으면 재생을 눌러주세요" 류의 복귀 안내 포함

### 반드시 포함할 주의 (⚠️)

주의 블록은 **수강생이 실제로 막히는 지점**에만 최소한으로 넣는다:

- **Play 중 값 소실**: 육성 대사까지 포함해서 명시 (VOD 질문 1위)
- **처음 겪는 팝업/자동 생성물**: TMP Essentials 임포트 창, Canvas 자동 생성 등 — 시연에 그대로 담고 안심 대사 병기
- 붙지 않음/목록에 안 보임 등 **대표 트러블슈팅 1~2개** — 원인을 한 가지로 좁혀서 안내

### 넣지 않는 것 (레퍼런스에서 의도적으로 제거된 요소)

- ❌ "강사님께" 류의 설계 철학·전제 설명 섹션 — 타임라인 표로 바로 시작한다
- ❌ 표기 규칙 범례 (🗣/🖥/⏸ 설명 블록)
- ❌ ✂️ 편집 지시 블록 — 자막 도식·확대 지시 등 편집 관련 내용은 꼭 필요한 것만 일반 bullet로 최소 표기
- ❌ 녹화 사전 체크리스트 부록
- ❌ 오프라인 → VOD 전환 차이표 부록
- ❌ 분량 조절 가이드 표 (한 줄 "분량이 남는다면"으로 대체)
- ❌ 진행 표시 고정 자막·구분 자막 카드 지시의 전면 배치 — 구분 자막은 필요한 구간에만 간헐적으로
- ❌ 강사의 실제 경험담 (특정 기수 언급 등) — 이건 강사가 직접 추가할 영역이므로 생성하지 않는다

---

## 워크플로우

변환 요청을 받으면 다음 순서로 진행한다:

1. **레퍼런스 2개를 먼저 읽는다** (`references/` 폴더)
2. 원본 교안을 읽고 다음을 분류한다:
   - 유지: 비유, 활용 사례, 개념의 뼈대
   - 치환: 표현 치환표 대상 용어, 코드로 설명된 개념 → Inspector 조작으로
   - 삭제: 삭제 대상 목록 해당 항목
   - 이동: 라이프사이클·문법 설명 → "다음 차시" 예고로
3. **실습 설계를 먼저 확정한다**: 이 차시에서 수강생이 마우스로 할 수 있는 실습 ①~④를 정한다. 실습이 성립하려면 해당 차시용 완성 스크립트(Inspector 노출 항목)가 필요할 수 있다 — 원본에 없으면 어떤 스크립트가 필요한지 명시하고 사용자에게 확인받는다
4. 노코드 교안 → VOD 스크립트 순서로 작성한다 (스크립트의 🖥 화면 지시가 교안 섹션을 참조하므로)
5. 두 산출물의 **실습 번호·순서·용어가 서로 일치하는지** 교차 검증한다

## 검증 체크리스트 (산출 전 자가 점검)

- [ ] 코드 블록이 "대응 관계 보여주기" 외에 남아 있지 않은가
- [ ] 사이트 전용 문법(`!!!` · `???` · `:material-`)이 남아 있지 않은가
- [ ] 치환표의 금지 용어가 본문에 남아 있지 않은가 (`grep` 으로 확인: 변수, 함수, 메서드, 상속, 컴파일, 매개변수, 프레임)
- [ ] 첫 문단에 "코드를 치지 않는다"가 있는가
- [ ] 실습마다 목표 / 순서 / 확인 / 트러블슈팅이 갖춰져 있는가
- [ ] Play 중 값 소실 경고가 교안(`> 🚨`)과 스크립트(육성 대사) 양쪽에 있는가
- [ ] 교안의 실습 번호와 스크립트의 ⏸ 실습 번호가 일치하는가
- [ ] 정리 문제의 정답이 접기(`<details>`) 처리되어 있는가
- [ ] 용어 정리 부록에 이번 차시 등장 용어가 모두 있는가
- [ ] 스크립트 타임라인 합계가 약 50분인가
- [ ] 라이선스 금액·버전 번호 등 시효성 정보가 본문에 박혀 있지 않은가

## 파일 배치

```
project/
├── CLAUDE.md                          ← 이 파일
├── references/
│   ├── 01_unity_engine_nocode.md      ← 골든 레퍼런스 (교안)
│   └── 01_unity_engine_vod_script.md  ← 골든 레퍼런스 (스크립트)
├── source/
│   └── NN_챕터명.md                    ← 변환할 원본 교안
└── output/
    ├── NN_챕터명_nocode.md
    └── NN_챕터명_vod_script.md
```
