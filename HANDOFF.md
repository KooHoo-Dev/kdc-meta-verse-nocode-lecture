# 인수인계 — 다른 PC에서 이어서 작업하기

> 이 문서 하나만 읽고도 이어서 작업할 수 있게 정리했습니다.
> **작성 시점: 2026-08-04** / 기준 커밋: `8b0afb9`
>
> *갱신 이력: `.meta` 이전 문제 해결(§3) · U-7 항목 수 정정(43 → 46) — 2026-08-04*

---

## 1. 30초 요약

**프로그래머용 Unity 교안을 비프로그래머·노코드용으로 변환하는 프로젝트**입니다.
KDC 메타버스 과정 **11 ~ 33차시(총 23차시)** 를 만들고 있습니다.

산출물은 차시당 두 개입니다.

| | |
| :--- | :--- |
| **노코드 교안** | 수강생이 영상과 함께 보는 자료 (`*_nocode.md`) |
| **VOD 녹화 스크립트** | 강사가 읽는 대본 (`*_vod_script.md`) |

**절대 원칙**: 수강생은 **코드를 한 줄도 치지 않습니다.** 완성된 부품(C# 스크립트)을 제공하고, **끌어다 놓기 · 값 바꾸기 · 목록에서 고르기**만으로 실습합니다.

### 지금 어디까지

```
11 ~ 21차시  ✅ 완료 (22개 문서, 배포 가능)
22 ~ 27차시  🟨 교안 6개 (VOD 없음) — 에디터 검증(U-7) 대기 · 배포 보류
28 ~ 31차시  ⬜ 미착수 — 지금 착수 가능 (XR 설계 전제 확정됨)
32 ~ 33차시  ⬜ 미착수 — U-7 대기 (클레이 사격의 VR 재구성이라)
교육용 컴포넌트 21개  ✅ 제작 완료
```

---

## 2. 새 세션에서 먼저 할 일

### 2-1. 읽을 문서 (이 순서로)

| 순서 | 문서 | 무엇이 |
| :---: | :--- | :--- |
| 1 | **이 문서** | 전체 현황 |
| 2 | `CLAUDE.md` | **프로젝트 상시 규칙** (변환 원칙 · 표현 치환표 · 금지 사항) |
| 3 | `plans/00_교안_생성_프롬프트.md` | **실행 규칙** (실습 설계법 · 고정 어휘집 · 자가 검증) |
| 4 | `plans/03_작업_우선순위.md` | 통합 백로그 · 의존 관계 |
| 5 | `references/01_unity_engine_nocode.md` <br> `references/01_unity_engine_vod_script.md` | **골든 레퍼런스** — 구조·톤의 기준 |

> 규칙이 충돌하면 우선순위: **`references/` > `plans/00` > `CLAUDE.md`**

### 2-2. 환경 세팅

```bash
pip install -r requirements-docs.txt
```

문서 빌드 검증에 필요합니다. 설치 후 아래가 통과해야 정상입니다.

```bash
mkdocs build --strict
```

> `git-revision-date-localized` 플러그인이 없으면 빌드가 아예 안 됩니다. **반드시 설치하세요.**

---

## 3. `.meta` 규칙 — 이전 문제는 해결됐습니다

### ✅ 해결 완료 — 다시 하지 마세요

`Assets/_NoCodeKit/` 의 `.meta` **23개**(스크립트 20 + 폴더 3)가 커밋 **`8b0afb9`** 에 전부 들어갔고 **푸시까지 끝났습니다.**

이제 **어느 PC에서 열어도 GUID가 같습니다.** 예전 판본에 있던 "한 대의 PC에서만 여세요" 절차는 **이미 끝난 작업**입니다.

### ⚠️ 다만 규칙 자체는 계속 유효합니다

앞으로 **씬 · 프리팹 · 폰트 · 재질을 새로 만들 때마다** 같은 문제가 생길 수 있습니다.

| | |
| :--- | :--- |
| Unity는 파일마다 `.meta` 로 **고유 ID(GUID)** 를 관리합니다 |
| `.meta` 를 빼고 커밋하면 다른 PC에서 **새 GUID가 생깁니다** |
| **GUID가 어긋나면** 씬·프리팹의 부품 연결이 **전부 끊어집니다** |

새 에셋을 만든 뒤에는 **`.meta` 를 짝으로 같이 커밋하세요.**

```bash
git status --short Assets
```

`.cs` · `.unity` · `.prefab` 옆에 **`.meta` 가 짝으로 보이는지** 확인하고 함께 커밋하면 됩니다.

> **U-6(씬 6~7개) · U-2(폰트) · U-3(프리팹)에서 새 파일이 많이 생깁니다.** 그때 한 번 더 확인하세요.

---

## 4. 진행 현황

### 4-1. 완료 — 11 ~ 21차시 (배포 가능)

| 차시 | 제목 | 위치 |
| :---: | :--- | :--- |
| 11 | 왜 메타버스에 유니티인가 | `Docs/1. NoCode/0. 시작하기/` |
| 12 | Unity 설치하고 첫 프로젝트 | 〃 |
| 13 | Unity 작업실 둘러보기 | `Docs/1. NoCode/1. Unity 기초/` |
| 14 | 부품을 붙여 자동차 만들기 | 〃 |
| 15 | 재료 넣기와 붕어빵 틀 | 〃 |
| 16 | 화면에 그리는 부품들 | 〃 |
| 17 | 화면 위에 올리는 것들 | `Docs/1. NoCode/2. UGUI/` |
| 18 | 누르고 움직이는 것들 | 〃 |
| 19 | 화면이 눌리는 원리와 조작 패널 | 〃 |
| 20 | X · Y · Z, 드디어 | `Docs/1. NoCode/3. 오브젝트 다루기/` |
| 21 | 떨어뜨리고 부딪히기 | 〃 |

각 차시 = 교안 1 + VOD 스크립트 1. **11차시만 실습이 없습니다** (설치 전이라 만질 게 없음).

### 4-2. 뼈대만 — 22 ~ 27차시 (클레이 사격)

`Docs/1. NoCode/4. 사격장/` 에 **교안 6개**가 있습니다. **VOD 스크립트는 아직 없습니다.**

!!! ⚠️ 자리는 배포 폴더 안이지만 **배포되지는 않습니다**
    `mkdocs.yml` 의 **`exclude_docs`** 로 막아뒀습니다. 🟨 표시가 남아 있는 동안은 공개하지 않습니다.
    **U-7 검증과 VOD 스크립트 6개가 나오면** 공개합니다 — 절차는 `plans/06_배포_공개_절차.md`.

| 부분 | 상태 |
| :--- | :--- |
| 제목 · 개념 절 · 부품 표 · 정리 문제 · 용어 부록 | ✅ 확정 |
| **실습 ①~④** | 🟨 **잠정** — 🟨 표시 17곳이 U-7 검증 대기 |
| VOD 스크립트 | ⬜ 미작성 (실습이 확정돼야 타임라인이 잡힘) |

### 4-3. 미착수 — 28 ~ 33차시 (XR)

!!! ✅ "Simulator 검증 대기" 는 해소됐습니다
    예전 판본은 *"실기기 없이 Simulator만으로 **실습이 성립하는지**"* 를 블로커로 잡았습니다.
    **그 질문은 성립하지 않습니다.** 아래가 확정된 설계입니다. (§6 결정 9)

    | | |
    | :--- | :--- |
    | **Simulator 의 역할** | **강사의 녹화 환경**입니다. 검증 대상이 아닙니다 |
    | **수강생 실습** | **선택**입니다. 안 따라 해도 차시가 무너지지 않습니다 |
    | **실기기 화면** | **별도 동영상**으로 제공합니다 (Quest 실행 장면) |
    | **전제** | **Simulator 로 한손 권총 클레이 사격을 테스트**합니다 |

| 구간 | 상태 |
| :--- | :--- |
| **28 ~ 31차시** (XR 기초) | **지금 착수 가능** — U-6/U-7 과 **병렬** |
| **32 · 33차시** (VR 클레이 사격) | **U-7 대기** — 22~27차시의 재구성이라 원본이 확정돼야 함 |

> **원본이 얇습니다.** `Docs/0. Origin/3. XR/` 에 3개뿐이고 **30차시는 원본이 아예 없습니다.**
> 22~27차시보다 새로 쓸 분량이 많습니다.

### 4-4. 교육용 컴포넌트 21개 (제작 완료)

```
Assets/_NoCodeKit/Scripts/
├── CubeSpinner.cs          14 · 19차시
├── ConsoleTester.cs        13차시
├── RendererSwitcher.cs     16 · 19차시
├── ValueDisplay.cs         18차시 이후 계속
├── TransformDriver.cs      20차시
├── CollisionReporter.cs    21차시
├── SkyboxSwitcher.cs       30차시
└── ClayShooting/           22 ~ 27차시 (14개)
    ├── PlayerMover.cs · MouseLook.cs
    ├── GameStarter.cs · TargetLauncher.cs · TargetLife.cs
    ├── SimpleGun.cs · Projectile.cs · MuzzleFlashPlayer.cs ⚠️
    ├── HitScorer.cs · Hittable.cs · FeedbackSpawner.cs
    ├── RoundTimer.cs · DifficultyPreset.cs
    └── AudioPlayer.cs ⚠️
```

> ⚠️ 두 개는 **U-7 검증 후 버릴 수 있습니다.** (§7 함정 ③ 참고)

---

## 5. 파일 배치

```
kdc-meta-verse-nocode-lecture/
├── HANDOFF.md              ← 이 문서
├── CLAUDE.md               ← 프로젝트 상시 규칙
│
├── Docs/
│   ├── 0. Origin/          원본 교안 25개 (프로그래머용, 배포 안 됨)
│   └── 1. NoCode/          ★ 배포 대상 (GitHub Pages)
│       ├── 0. 시작하기/      11 · 12       ✅ 배포 중
│       ├── 1. Unity 기초/    13 ~ 16       ✅ 배포 중
│       ├── 2. UGUI/          17 ~ 19       ✅ 배포 중
│       ├── 3. 오브젝트 다루기/ 20 · 21       ✅ 배포 중
│       ├── 4. 사격장/         22 ~ 27       ⛔ exclude_docs 로 보류
│       └── 5. XR/            28 ~ 31       ⛔ exclude_docs 로 보류
│
├── plans/                  계획 문서 7개
├── references/             골든 레퍼런스 (기존 1차시, 손대지 말 것)
├── source/                 변환 대기 원본
├── tools/                  한글 문자셋 2350자 + 폰트 제작 절차
│
└── Assets/_NoCodeKit/      교육용 컴포넌트 21개
```

### 배포 구조

`mkdocs.yml` 의 `docs_dir` 이 **`Docs/1. NoCode`** 입니다.
따라서 `plans/` · `references/` · `tools/` 는 **사이트에 안 올라갑니다.**

목차는 폴더별 **`.pages`** 파일로 제어합니다 (awesome-pages 플러그인).

### ⛔ 배포 보류 — `exclude_docs`

**22 ~ 31차시는 배포 폴더 안에 있지만 사이트에는 안 올라갑니다.**
🟨 표시(미확정 지점)가 남아 있어서 `mkdocs.yml` 의 **`exclude_docs`** 로 막아뒀습니다.

| 폴더 | 푸는 조건 |
| :--- | :--- |
| `4. 사격장/` | **U-7** 검증 완료 + VOD 스크립트 6개 작성 |
| `5. XR/` | **U-9** 검증 완료 (🟨 는 클릭 경로뿐) |

**파일 이동은 필요 없습니다.** 다만 **`exclude_docs` 만 지우면 끝이 아닙니다.**

| | 고칠 곳 | 안 고치면 |
| :---: | :--- | :--- |
| 1 | `mkdocs.yml` 의 `exclude_docs` | 페이지가 안 만들어짐 |
| 2 | `Docs/1. NoCode/.pages` 의 `nav:` | **좌측 메뉴에 안 뜸** |
| 3 | `Docs/1. NoCode/index.md` 강의 목록 | 홈에서 못 찾음 |

**순서를 지켜야 합니다.** 1번을 건너뛰고 2번만 하면 `NavEntryNotFound` 로 빌드가 깨집니다.

> **전체 절차와 붙여넣기용 블록: `plans/06_배포_공개_절차.md`**
> 폴더별 `.pages` 는 **이미 만들어 두었습니다.** 손댈 필요 없습니다.

---

## 6. 확정된 결정 — 다시 논의하지 마세요

이미 사용자와 합의된 사항입니다. **바꾸려면 파급을 먼저 계산하세요.**

| # | 결정 | 근거 |
| :---: | :--- | :--- |
| 1 | **파일 번호 = 시트 차시 번호(11~33)** | 시트와 1:1 대응 |
| 2 | 기존 1차시는 **11 / 12 / 14 / 19 로 분해** | 설치 전에 실습시키는 구조가 성립 안 함 |
| 3 | **클레이 사격은 처음부터 제작** (기존 프로젝트 없음) | |
| 4 | **기본 도형 위주 + 최소 무료 에셋** | 라이선스·용량 |
| 5 | **버튼을 14차시에 도입** (원래 19차시) | 골든 레퍼런스의 클라이맥스를 9차시나 미룰 수 없음 |
| 6 | **난이도는 버튼 3개** (`DifficultyPreset`) | 값 직접 입력보다 게임다움 |
| 7 | **총알은 실제로 날아가는 물건** | 즉시 판정은 눈에 안 보여 21차시와 단절 |
| 8 | **26차시에서 시작 방식이 발판 → 버튼** | 난이도 버튼과 커서 잠금이 충돌 (함정 ②) |
| 9 | **XR: Simulator = 녹화 환경 · 수강생 실습은 선택 · 실기기는 별도 영상** | 아래 참고 |

#### 결정 9 — XR 설계 전제 (28 ~ 33차시)

**"Simulator 로 한손 권총을 이용해 클레이 사격을 테스트한다"** 가 전제입니다.

| 항목 | 확정 내용 |
| :--- | :--- |
| 녹화 | 강사가 **XR Device Simulator 로 녹화**합니다 |
| 수강생 실습 | **따라 할지 말지 수강생이 고릅니다** (선택) |
| 실기기 | **MetaQuest 실행 화면은 별도 동영상**으로 제공합니다 |
| 교안 실습 절 | 기존 **실습 ①~④ 구조를 유지**하되, 앞머리에 **"눈으로 보셔도 됩니다"** 안내를 답니다 |

> 그래서 *"Simulator 만으로 실습이 성립하는가"* 는 **착수 조건이 아닙니다.** 28~31차시는 지금 쓸 수 있습니다.

### 아직 안 정해진 것

| # | 결정 대기 | 어떻게 정해지나 |
| :---: | :--- | :--- |
| 1 | `MuzzleFlashPlayer` · `AudioPlayer` 를 쓸지 | **U-7 `3-6` · `6-2`** 로만 답이 나옴 |

---

## 7. ⚠️ 반드시 알아야 할 함정 5개

### 함정 ① — 프리팹은 씬 안의 물건을 참조할 수 없습니다

표적은 프리팹인데 점수판은 씬에 하나입니다. **끌어다 놓으려 해도 칸이 안 받습니다.**

**해결(구현 완료)**: `HitScorer` 를 씬에 하나 두고, `Hittable` 이 **내부적으로 찾아갑니다.**
교안에서는 **축구 심판** 비유로 설명합니다 — *"선수가 각자 점수판을 들고 다니지 않는다"*.

### 함정 ② — 마우스를 잠그면 UI 버튼을 못 누릅니다

1인칭 조준은 커서 잠금이 필요한데, 난이도 버튼은 커서가 필요합니다.

**해결(설계 완료)**: 26차시에서 **시작 방식을 발판 → 버튼으로** 전환합니다.
`GameStarter.StartGame` 은 **트리거로도 버튼으로도** 발동하게 만들어두었습니다.

### 함정 ③ — 이미 있는 기능을 새로 만들지 마세요

`AudioSource.Play()` · `ParticleSystem.Play()` 가 `On Click` 드롭다운에 **나오면**
`AudioPlayer` · `MuzzleFlashPlayer` 는 **안 만들어도 됩니다.**

**U-7 `3-6` · `6-2` 두 줄만 확인하면 컴포넌트 2개가 사라집니다.**

### 함정 ④ — 이 프로젝트는 새 Input System 전용입니다

`ProjectSettings` 의 `activeInputHandler: 1` 입니다.
**옛 방식(`Input.GetAxis`)을 쓰면 실행 즉시 예외가 터집니다.**

기존 컴포넌트 3개(`PlayerMover` · `MouseLook` · `SimpleGun`)는 **양쪽 다 컴파일되게** 만들어두었습니다.
**새 컴포넌트를 만들 때도 같은 방식(`#if ENABLE_INPUT_SYSTEM`)을 쓰세요.**

### 함정 ⑤ — 한글 폰트 마감이 12차시로 당겨졌습니다

**14차시 실습 ④에서 버튼 글자를 "멈춤"으로 씁니다.** 폰트가 없으면 거기서부터 네모(☐)입니다.

원래 18차시 마감이었는데 **버튼이 14차시로 앞당겨지면서 같이 당겨졌습니다.**
→ **12차시 강의자료(`.unitypackage`)에 폰트가 들어가야 합니다.**

---

## 8. 지금 해야 할 일

### 🔴 우선순위 1 — 에디터 작업 (병목)

문서 작업이 여기서 막혀 있습니다. **Unity 에디터가 필요합니다.**

| # | 할 일 | 막고 있는 것 | 참고 |
| :---: | :--- | :--- | :--- |
| **U-2** | 한글 폰트 SDF 제작 | 12차시 배포본 | `tools/README.md` (문자셋 2350자 준비됨) |
| **U-3a** | `Assets/_NoCodeKit/` → `.unitypackage` | 13차시 녹화 | |
| **U-3b** | 13 ~ 21차시용 씬 | 녹화 | |
| **U-6** | **클레이 사격 완성본 제작** | 22~27 교안 | `plans/04_클레이사격_설계.md` §3 |
| **U-7** | **마우스 조작 검증 46항목** | 22~27 교안 | **`plans/05_U6_U7_에디터_작업지시.md`** ★ |
| **U-9** | XR 패키지 설치 + XR Origin 프리팹 | 28~31 **녹화** | 교안 집필은 안 막습니다 |

> **U-7이 가장 중요합니다.** ✅ / ⚠️ / ❌ 로 채우면, `❌` 항목이 곧 컴포넌트 수정 명세가 됩니다.
> 체크리스트에 **일부러 넣은 항목**이 하나 있습니다 — `4-4` 는 **❌ 가 나와야 정상**입니다 (함정 ① 확인용).

### 🟠 우선순위 2 — U-7 결과가 나오면

1. `❌` 항목에 맞춰 **컴포넌트 수정**
2. `Docs/1. NoCode/4. 사격장/` 의 **🟨 표시 17곳**을 실제 결과로 확정
3. **VOD 스크립트 6개 작성** (22~27)
4. `4. 사격장/.pages` 생성 + 루트 `.pages` 의 `nav:` 에 추가 + `index.md` 등록
5. `mkdocs.yml` 의 `exclude_docs` 에서 **`4. 사격장/` 줄 삭제**
6. `mkdocs build --strict` + 교차 검증

### 🟡 우선순위 3 — 28 ~ 31차시 (지금 착수 가능 · 병렬)

**교안 8개.** XR 설계 전제가 확정돼 있으므로 (§6 결정 9) **U-6/U-7 과 동시에 진행할 수 있습니다.**
U-6/U-7 은 사람이 Unity 앞에 있어야 하고, 이쪽은 문서 작업이라 서로 안 막습니다.

**32 · 33차시**는 클레이 사격의 VR 재구성이라 **U-7 결과를 기다립니다.**

---

## 9. 작업 방법

### 9-1. 차시 하나를 만드는 절차

`plans/00_교안_생성_프롬프트.md` §A 의 프롬프트를 씁니다. 요지는:

1. `references/` 골든 레퍼런스 2개를 **먼저 읽는다**
2. **직전 차시 산출물**을 읽고 용어·실습 번호·예고를 확인한다
3. **실습 ①~④를 먼저 확정한다** (난이도 계단: 확인 → 값 조작 → **결과물** → 심화)
4. **노코드 교안 → VOD 스크립트** 순으로 쓴다
5. 자가 검증을 전부 돌린다

> **실습 ③ 이 그 차시의 제목입니다.** ③이 안 잡히면 그 차시는 설계가 안 된 것입니다.

### 9-2. 자가 검증

```bash
# 1. 빌드 (경고 0건이어야 통과)
mkdocs build --strict
```

```bash
# 2. 금지 용어 — 문서 말미 치환표 안에만 걸려야 통과
grep -nE "변수|필드|메서드|함수|매개변수|상속|컴파일|프레임|클래스|인스턴스|렌더링" "Docs/1. NoCode"/**/*.md
```

```bash
# 3. C# 코드블록 — 0건이어야 통과
grep -rn '```csharp' "Docs/1. NoCode"
```

**수동 대조 4가지**

- [ ] 교안 실습 번호 `①~④` ↔ 스크립트 `⏸ 실습` 번호 일치
- [ ] 정리 문제 개수 == `??? success "정답` 개수 (**5문항 고정**)
- [ ] VOD 타임라인이 `00:00` → `00:50` **빈틈·중복 없이** 연속
- [ ] Play 중 값 소실 경고가 교안 `!!! danger` + 스크립트 육성 대사 **양쪽**에

### 9-3. 고정 어휘 (절대 바꾸지 말 것)

노코드 수강생은 **용어가 바뀌면 다른 개념으로 인식**합니다.

| 화면 이름 | 고정 표현 | | 화면 이름 | 고정 표현 |
| :--- | :--- | :--- | :--- | :--- |
| GameObject | **빈 그릇** | | Project | **재료함** |
| Component | **부품** | | Console | **메모장** |
| Transform | **위치 부품** | | Material | **색깔·질감 재료** |
| Mesh Filter | **모양 부품** | | Mesh Renderer | **보이는 부품** |
| Rigidbody | **떨어지는 부품** | | Collider | **부딪히는 부품** |
| Inspector | **조종석** | | Prefab | **붕어빵 틀** |
| Scene | **작업대** | | 부모·자식 | **묶은 것·묶인 것** |
| Game | **미리보기** | | Hierarchy | **놓인 것들의 목록** |

전체 목록은 `plans/00_교안_생성_프롬프트.md` §D-1 에 있습니다. **새 용어를 확정하면 그 표에 먼저 추가한 뒤** 본문을 쓰세요.

---

## 10. 알아두면 좋은 것들

### 반복되는 교육 장치

| 장치 | 설명 |
| :--- | :--- |
| **실패 장면을 자르지 않는다** | 강사가 실수하고 복구하는 걸 그대로 담습니다 |
| **결과물을 오프닝에 먼저** | "오늘 이걸 만드십니다"가 50분을 버티게 합니다 |
| **"안 되셔도 괜찮습니다"** | 난이도가 아니라 **'뭘 해야 할지 모름'** 이 불안의 원인 |
| **미해결 질문을 다음 차시로** | 14차시의 "0.55는 어느 쪽?" → 20차시에서 회수 |
| **답하지 마세요** | 문법·이론 질문은 VOD 예상 질문 표에 `"N차시에서 다룹니다"로 통일` |

### 회수 구조 (설계의 뼈대)

22~27차시 클레이 사격은 **11~21차시를 하나도 안 버리고** 되받습니다.

| 배운 것 | 게임에서 |
| :--- | :--- |
| 부모-자식 (14) | 22차시 **몸→눈**, 24차시 **몸→눈→총** |
| 프리팹 (15) | 표적·총알·효과 + 25차시의 제약 |
| 파티클 (16) | 총구 불꽃·명중 효과 |
| `Dynamic float` (18) | 25차시 점수, 26차시 시간 |
| X·Y·Z (20) | 발사 각도·총알 방향 |
| Is Trigger + Tag (21) | 시작 발판·명중 판정 |

### 아직 안 만든 것 하나

**11차시 오프닝의 완성작 투어 영상**입니다.
VOD 스크립트에 `<완성본 플레이 영상 — U-11에서 채움>` 으로 비어 있습니다.

클레이 사격(P3)과 VR(P4)이 끝나야 찍을 수 있어서, **11차시는 교안이 가장 먼저 나왔지만 녹화는 맨 마지막**입니다. 일정에 반영하세요.

---

## 11. 한 줄 요약

> **문서 작업은 11~21차시까지 끝났고, 22~27차시는 뼈대만 있습니다.**
> **지금 필요한 건 Unity 에디터에서 `plans/05_U6_U7_에디터_작업지시.md` 의 46개 항목을 검증하는 것입니다.**
> **그 결과가 나오면 22~27차시를 확정하고, 이어서 32·33차시로 갑니다.**
> **28~31차시는 아무것도 안 기다립니다 — 지금 쓸 수 있고, 위 검증과 병렬로 가면 됩니다.**
